from util import modinv

def point_double(x, y, p, A):
    k = ((3*x1**2 + A)*modinv(2*y1, p))%p
    x3 = (k**2 - 2*x1)%p
    y3 =

def point_add(x1, y1, x2, y2, p, A):
    if
    k = ((3*x1**2 + A)*modinv(2*y1, p))%p
    x3 = (k**2 - 2*x1)%p
    y3 =